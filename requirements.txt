# Install into repo-root venv only: ./setup_python_env.sh
# Optional tooling for export_comic_detection_to_coreml.py (run on macOS).
coremltools>=8.0
# coremltools 9.x is validated through torch 2.7.0; newer torch may warn (often still works).
torch>=2.0,<2.8
# ultralytics>=8.0  # uncomment if exporting from YOLOv8 weights
