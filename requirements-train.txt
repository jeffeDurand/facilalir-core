# Training + Core ML export for comic bubble YOLO (separate venv recommended).
# Install: ./setup_train_env.sh   OR   python3 -m venv .venv-train && .venv-train/bin/pip install -r requirements-train.txt
#
# Why a second env? Ultralytics often pulls a newer torch than the coremltools-focused
# requirements.txt pin; keeping `.venv` for light checks and `.venv-train` for training avoids conflicts.

python-dotenv>=1.0.0
ultralytics>=8.3.0
huggingface_hub>=0.20.0
