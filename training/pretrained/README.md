# Downloaded pretrained weights (gitignored)

`.pt` files saved by `train_comic_bubble_yolo.py download-pretrained` live here by default.

Do not commit weights to git. Prefer `export-pretrained-coreml` which uses the Hugging Face cache and writes only the Core ML bundle under `assets/`.
